# App
spring.application.name=kanban-board-sa

# --- DataSource (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5433/kanban_board
spring.datasource.username=josip
spring.datasource.password=Pa55w.rd
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- JWT (matches JwtUtil fields) ---
# IMPORTANT: this MUST be a BASE64-encoded secret for HS256 (>= 256-bit).
# Generate one (Linux/macOS):  openssl rand -base64 64
security.jwt.secret=PericaUZitu123sjdsdksjhdjsklhdajklsdhjklsahjkldMilica
security.jwt.access-ttl-minutes=15

# Disable default basic user
spring.security.user.password=disabled

# --- CORS (adjust for your frontend ports as needed) ---
cors.allowed-origins=http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type
cors.exposed-headers=Authorization

# --- File upload limits (profile images) ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# --- AWS S3 (fill in your values) ---
aws.accessKey=
aws.secretKey=
aws.region=
aws.s3.bucket=

# --- Scheduling (used by RefreshTokenMaintenanceServiceImpl) ---
scheduling.tokens.purge.cron=0 0 */2 * * *
scheduling.tokens.stats.cron=0 5 0 * * *
scheduling.zone=Europe/Zagreb

# --- Logging ---
logging.level.com.keresman.kanbanboard=INFO
